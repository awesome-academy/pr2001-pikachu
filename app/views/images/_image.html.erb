<% if image == tour.images.first %>
<div class="carousel-item active">
  <%= image_tag(image&.link_url.to_s, width: "1300", height: "500") %>
</div>
<% else %>
<div class="carousel-item">
  <%= image_tag(image.link_url.to_s, width: "1300", height: "500") %>
</div>
<% end %>
